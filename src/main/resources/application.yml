spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: sample_training
      username: demouser
      password: bk2rFn*d

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: TRACE